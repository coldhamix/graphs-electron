import { ChartDrawerDirective } from './chart-drawer.directive';

describe('ChartDrawerDirective', () => {
  it('should create an instance', () => {
    const directive = new ChartDrawerDirective();
    expect(directive).toBeTruthy();
  });
});
